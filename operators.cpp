#include <iostream>

int main()
{
    int x = 5 + 5; // assignment operator; addition operator
    
    //common operators are the same as the ones in Java
    
    //int y = 10/4 // gives 2 because nums are not doubles

    double y = 10.0 / 4; // now gives double answer (2.5)

    std::cout << y << std::endl;

    double z = 10 % 4; // modulus gives the remainder in a division (2)

    std::cout << z << std::endl;

}